import { describe, it, expect } from 'vitest';
import * as cli from '../src/index';

describe.skip('CLI Entrypoint', () => {
  it('should export something', () => {
    expect(true).toBe(true);
  });
});
