name: Waivers
on:
  pull_request:
jobs:
  waivers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/check-waivers.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: waivers
          path: reports/waivers.md
